# Secret / credential files — NEVER commit these
.env
.env.local
.env.*.local

# .NET build output
backend/bin/
backend/obj/

# Docker volumes (if mounted locally)
data/

# OS / editor
.DS_Store
Thumbs.db
.vscode/settings.json
*.user
